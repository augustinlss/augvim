return {
  -- Auto close html tags (already included in treesitter but keeping for compatibility)
  {
    "windwp/nvim-ts-autotag",
    event = "VeryLazy",
    config = function()
      require("nvim-ts-autotag").setup()
    end,
  },
}
